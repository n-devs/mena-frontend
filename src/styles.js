import React from 'react';
import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
      "@global": {
            ".MuiAppBar-colorPrimary" :{
                  color: "#fff",
                  backgroundColor: "#F6D357",
              }
      }
});

export default useStyles